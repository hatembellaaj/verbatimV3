{
  "name": "doria-profiler-api",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "API DORIA — proxy Anthropic + persistance Postgres (catégories, projets, ancres)",
  "main": "index.mjs",
  "scripts": {
    "start": "node index.mjs"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0"
  },
  "engines": {
    "node": ">=20"
  }
}
